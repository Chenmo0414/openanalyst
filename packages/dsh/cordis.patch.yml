# The configuration layer applied when a profile lists this bundle.
# The client half is discovered from the package's `dsh.client` declaration,
# so only the host plugin row belongs here.
- insert:
    - id: tukey
      name: tukey
